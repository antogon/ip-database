<%= form_for(@dhcp_range) do |f| %>
  <% if @dhcp_range.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dhcp_range.errors.count, "error") %> prohibited this dhcp_range from being saved:</h2>

      <ul>
      <% @dhcp_range.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
